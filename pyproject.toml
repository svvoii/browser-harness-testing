[project]
name = "browser-harness-testing"
version = "0.1.0"
description = "AI-driven E2E testing system"
dependencies = [
    "browser-harness",
    "PyGithub",
]

[project.optional-dependencies]
dev = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"